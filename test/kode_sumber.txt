program Hello;

var
  a, b, i: integer;

function AddTen(x: integer): integer;
begin
  AddTen := x + 10;
end;

begin
  a := 5;
  b := AddTen(a);
  writeln('Result = ', b);

  for i := 1 to 5 do
  begin
    writeln('Loop = ', i);
  end;
end.